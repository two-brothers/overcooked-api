Ingredient:
  required:
    - id
    - name
    - namePlural
  properties:
    id:
      type: string
    name:
      type: string
    namePlural:
      type: string

Ingredients:
  type: array
  items:
    $ref: '#/definitions/Ingredient'

Name:
  required:
    - name
    - namePlural
  properties:
    name:
      type: string
    namePlural:
      type: string

Error:
  properties:
    code:
      type: string
    message:
      type: string